-- Iron Soul Hub (Rayfield Gen2) - Kill Aura / Auto Attack
-- Game: Iron Soul: Dungeon | PlaceId: 117533937949084 | UniverseId: 9910245722
-- Executor: SolaraV3
-- Load Rayfield Gen2 Stable: https://sirius.menu/gen2
-- Docs: https://docs.sirius.menu/rayfield-gen2 (window/tabs/elements/saving/secure-mode)
-- WARNING: "Cheating will result in PERMANENT BAN". Test di private server / alt dulu.

-- Develop dengan secure OFF, release ON (error di-swallow kalau ON).
-- getgenv().RAYFIELD_SECURE = true
getgenv().RAYFIELD_SECURE = false

local Rayfield = loadstring(game:HttpGet("https://sirius.menu/gen2"))()

-- Config (sama key dengan versi console, biar kompatibel)
getgenv().KillAuraConfig = getgenv().KillAuraConfig or {
    Enabled = false,
    UnlimitedRange = true,  -- true = deteksi musuh seluruh map (datangi otomatis)
    Radius = 60,            -- dipakai hanya kalau UnlimitedRange = false
    AttackRange = 12,       -- jarak natural senjata: pukulan keluar hanya kalau musuh sedekat ini
    AttackDelay = 0.25,     -- jeda antar serangan (detik)
    PositionMode = "Inside",-- "Inside" = di dalam musuh sejajar tanah (seperti script Tora IsMe), "Above" = di atas kepala
    Distance = 5,           -- jarak player dari musuh (mode Inside), meniru "Set Distance"
    HeightOffset = 8,       -- jarak naik-turun di atas kepala musuh (mode Above)
    AutoEquip = true,       -- auto equip Weapon dari Backpack
    MaxTargets = 10,        -- max musuh per tick (cegah lag)
    AutoSkill = false,      -- auto pakai skill Q/E/R bergantian
    SkillDelay = 3,         -- jeda antar skill (detik)
    WalkSpeedLock = false,  -- kunci WalkSpeed
    WalkSpeed = 16,         -- nilai WalkSpeed (default game 16/25)
    FindChestEgg = false,   -- auto ambil Chest & Dragon Egg (jalan kalau tidak ada musuh)
}
local Config = getgenv().KillAuraConfig
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function getCharacter()
    local c = LocalPlayer.Character
    if c and c:FindFirstChildOfClass("Humanoid") and c:FindFirstChildOfClass("Humanoid").Health > 0 then
        return c
    end
    return nil
end

local function getHRP(char)
    return char and char:FindFirstChild("HumanoidRootPart") or nil
end

local function ensureWeapon()
    local char = getCharacter()
    if not char then return nil end
    for _, t in ipairs(char:GetChildren()) do
        if t:IsA("Tool") and t.Name == "Weapon" then return t end
    end
    if Config.AutoEquip then
        local bp = LocalPlayer:FindFirstChild("Backpack")
        if bp then
            local tool = bp:FindFirstChild("Weapon")
            if tool then
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hum then pcall(function() hum:EquipTool(tool) end) end
                return tool
            end
        end
    end
    return nil
end

local function getEnemyPivot(model)
    -- Kembalikan CFrame + posisi musuh walau tanpa HumanoidRootPart (kasus dungeon: model GUID tanpa HRP)
    local hrp = model:FindFirstChild("HumanoidRootPart")
    if hrp and hrp:IsA("BasePart") then return hrp.CFrame, hrp.Position, hrp end
    local pp = model.PrimaryPart
    if pp and pp:IsA("BasePart") then return pp.CFrame, pp.Position, pp end
    for _, c in ipairs(model:GetChildren()) do
        if c:IsA("BasePart") then return c.CFrame, c.Position, c end
    end
    local ok, cf = pcall(function() return model:GetPivot() end)
    if ok and cf then return cf, cf.Position, nil end
    return nil, nil, nil
end

local function findEnemies(myPos)
    local list = {}
    -- Prioritas 1: folder dungeon (cepat + tidak kena NPC lobby)
    local priorityRoots = {}
    local we = workspace:FindFirstChild("WorldEnemys")
    if we then table.insert(priorityRoots, we) end
    local en = workspace:FindFirstChild("EnemyNpc")
    if en then table.insert(priorityRoots, en) end
    local scopes = (#priorityRoots > 0) and priorityRoots or { workspace }
    for _, scope in ipairs(scopes) do
        for _, v in ipairs(scope:GetDescendants()) do
            local ok, info = pcall(function()
                if not v:IsA("Humanoid") then return nil end
                if v.Health <= 0 then return nil end
                local model = v.Parent
                if not model or not model:IsA("Model") then return nil end
                if model == LocalPlayer.Character then return nil end
                if Players:GetPlayerFromCharacter(model) then return nil end
                local cf, pos, part = getEnemyPivot(model)
                if not pos then return nil end
                local dist = (pos - myPos).Magnitude
                -- UnlimitedRange = true -> radius mati, selagi map ada musuh langsung target
                if not Config.UnlimitedRange and dist > Config.Radius then return nil end
                return { Model = model, Humanoid = v, Root = part, Pos = pos, Dist = dist }
            end)
            if ok and info then
                table.insert(list, info)
                if #list >= Config.MaxTargets then break end
            end
        end
        if #list >= Config.MaxTargets then break end
    end
    table.sort(list, function(a, b) return a.Dist < b.Dist end)
    return list
end

local function attackOnce()
    local char = getCharacter()
    if not char then return 0 end
    local myRoot = getHRP(char)
    if not myRoot then return 0 end
    local tool = ensureWeapon()
    local enemies = findEnemies(myRoot.Position)
    if #enemies == 0 then return 0 end
    -- Player datang ke musuh terdekat.
    -- Mode Inside (meniru script yg work): sejajar tanah di dalam musuh, offset Set Distance.
    -- Mode Above: teleport ke atas kepala musuh (jarak naik-turun bisa diatur).
    local first = enemies[1]
    if first and first.Pos then
        if Config.PositionMode == "Above" then
            pcall(function()
                myRoot.CFrame = CFrame.new(first.Pos + Vector3.new(0, Config.HeightOffset, 0))
                myRoot.Velocity = Vector3.new(0, 0, 0)
            end)
        else
            pcall(function()
                local dir = myRoot.Position - first.Pos
                dir = Vector3.new(dir.X, 0, dir.Z)
                if dir.Magnitude < 0.5 then dir = Vector3.new(0, 0, 1) else dir = dir.Unit end
                local targetPos = first.Pos + dir * Config.Distance
                -- samakan tinggi pivot musuh (HRP ~2.5 di atas tanah) + hadap musuh biar natural
                myRoot.CFrame = CFrame.new(Vector3.new(targetPos.X, first.Pos.Y, targetPos.Z), first.Pos)
                myRoot.Velocity = Vector3.new(0, 0, 0)
            end)
        end
    end
    -- Serangan NATURAL: pukul hanya musuh yg masuk jarak senjata (bisa diatur jauh-dekat).
    -- Biar ayunan tidak keluar saat musuh masih jauh (kelihatan bot).
    local myPos = myRoot.Position
    local inRange = {}
    for _, e in ipairs(enemies) do
        local d = (e.Pos - myPos).Magnitude
        if d <= Config.AttackRange then table.insert(inRange, e) end
    end
    if #inRange == 0 then return 0 end
    -- Auto hit TANPA visual hit (tidak ubah Size/Transparency musuh sama sekali)
    if tool then pcall(function() tool:Activate() end) end
    pcall(function()
        local rs = game:GetService("ReplicatedStorage")
        local remotes = rs:FindFirstChild("Remotes")
        local act = remotes and remotes:FindFirstChild("PlayerActionRE")
        if act then act:FireServer() end
    end)
    return #inRange
end

-- Auto skill Q/E/R bergantian (tombol skill di HUD). Pakai VirtualInput, tanpa remote khusus.
local SkillKeys = { Enum.KeyCode.Q, Enum.KeyCode.E, Enum.KeyCode.R }
local SkillIndex = 1
local function castNextSkill()
    local vim = game:GetService("VirtualInputManager")
    local key = SkillKeys[SkillIndex]
    SkillIndex = SkillIndex % #SkillKeys + 1
    pcall(function()
        vim:SendKeyEvent(true, key, false, game)
        task.wait(0.05)
        vim:SendKeyEvent(false, key, false, game)
    end)
end

-- Find Chest & Dragon Egg: datangi yg terdekat + fire semua ProximityPrompt di dalamnya.
-- Dijalankan hanya kalau tidak ada musuh (biar tidak rebutan teleport dengan Kill Aura).
local function collectChestEgg()
    local char = getCharacter()
    local myRoot = char and getHRP(char)
    if not myRoot then return false end
    local myPos = myRoot.Position
    local best, bestDist = nil, math.huge
    local roots = {}
    local tc = workspace:FindFirstChild("TreasureChests")
    if tc then table.insert(roots, tc) end
    local de = workspace:FindFirstChild("DragonEggs")
    if de then table.insert(roots, de) end
    if #roots == 0 then roots = { workspace } end
    for _, scope in ipairs(roots) do
        for _, v in ipairs(scope:GetDescendants()) do
            if v:IsA("ProximityPrompt") and v.Enabled then
                local holder = v.Parent
                local part = holder and (holder:IsA("BasePart") and holder or holder:FindFirstChildWhichIsA("BasePart", true))
                if part then
                    local d = (part.Position - myPos).Magnitude
                    if d < bestDist then best, bestDist = part, d end
                end
            end
        end
    end
    if not best then return false end
    -- Dekat ke prompt (3 studs), tunggu prompt ke-stream, lalu fire.
    -- fireproximityprompt men-trigger LANGSUNG walau prompt-nya tipe "tahan F" (HoldDuration di-skip).
    pcall(function()
        myRoot.CFrame = CFrame.new(best.Position + Vector3.new(0, 3, 0))
        myRoot.Velocity = Vector3.new(0, 0, 0)
    end)
    task.wait(0.6)
    pcall(function()
        local holder = best.Parent
        local scope = holder and holder.Parent or workspace
        for _, p in ipairs(scope:GetDescendants()) do
            if p:IsA("ProximityPrompt") and p.Enabled then
                local pp = p.Parent
                local part = pp and (pp:IsA("BasePart") and pp or pp:FindFirstChildWhichIsA("BasePart", true))
                if part and (part.Position - myRoot.Position).Magnitude < 20 then
                    fireproximityprompt(p)
                    task.wait(0.2)
                    -- tembak 2x untuk prompt tahan-lama yg rewel
                    if p.Enabled then fireproximityprompt(p) end
                end
            end
        end
    end)
    return true
end

getgenv().KillAuraLoopRunning = getgenv().KillAuraLoopRunning or false
getgenv().KillAuraTotalHits = getgenv().KillAuraTotalHits or 0

-- Forward declarations buat UI handles (diisi setelah window jadi)
local ui = {
    statTargets = nil,
    statHits = nil,
    console = nil,
    statusText = nil,
}
local windowRef = nil

local function logLine(s)
    pcall(function()
        if ui.console then ui.console:Append(s) end
    end)
    print("[KillAura] " .. tostring(s))
end

local function startLoop()
    if getgenv().KillAuraLoopRunning then return end
    getgenv().KillAuraLoopRunning = true
    task.spawn(function()
        local tick = 0
        while getgenv().KillAuraLoopRunning do
            -- Kunci WalkSpeed (game suka reset)
            if Config.WalkSpeedLock then
                pcall(function()
                    local c = getCharacter()
                    local h = c and c:FindFirstChildOfClass("Humanoid")
                    if h and h.WalkSpeed ~= Config.WalkSpeed then h.WalkSpeed = Config.WalkSpeed end
                end)
            end
            local didAttack = false
            if Config.Enabled then
                local ok, n = pcall(attackOnce)
                if ok and n > 0 then
                    getgenv().KillAuraTotalHits = getgenv().KillAuraTotalHits + n
                    didAttack = true
                end
            end
            -- Chest & Egg hanya jalan kalau tidak ada musuh yg diserang
            if Config.FindChestEgg and not didAttack then
                local now = os.clock()
                getgenv().KillAuraNextChest = getgenv().KillAuraNextChest or 0
                if now >= getgenv().KillAuraNextChest then
                    getgenv().KillAuraNextChest = now + 2
                    pcall(collectChestEgg)
                end
            end
            tick = tick + 1
            -- Auto skill tiap SkillDelay detik (terpisah dari attack loop)
            if Config.Enabled and Config.AutoSkill then
                local now = os.clock()
                getgenv().KillAuraNextSkill = getgenv().KillAuraNextSkill or 0
                if now >= getgenv().KillAuraNextSkill then
                    getgenv().KillAuraNextSkill = now + Config.SkillDelay
                    pcall(castNextSkill)
                end
            end
            -- update Stat tiap ~1 detik (AttackDelay 0.25 -> tiap 4 tick)
            if tick % 4 == 0 then
                pcall(function()
                    local char = getCharacter()
                    local myPos = char and getHRP(char) and getHRP(char).Position or nil
                    local count = 0
                    if myPos then count = #findEnemies(myPos) end
                    if ui.statTargets then ui.statTargets:Set(count) end
                    if ui.statHits then ui.statHits:Set(getgenv().KillAuraTotalHits) end
                    if ui.statusText then
                        local scope = Config.UnlimitedRange and "map" or (tostring(Config.Radius) .. " studs")
                        if not Config.Enabled then
                            ui.statusText:Set("Idle. ON-kan toggle, player otomatis ke atas kepala musuh.")
                        elseif count == 0 then
                            ui.statusText:Set("ON - 0 target di " .. scope .. ". Masuk dungeon biar musuh spawn.")
                        else
                            ui.statusText:Set("ON - " .. tostring(count) .. " target (" .. scope .. "). Total hits: " .. tostring(getgenv().KillAuraTotalHits))
                        end
                    end
                end)
            end
            task.wait(Config.AttackDelay)
        end
    end)
end

local function stopLoop(restore)
    getgenv().KillAuraLoopRunning = false
    Config.Enabled = false
    -- tidak ada visual yg diubah (tanpa visual hit), jadi tidak ada yg perlu di-restore
end

getgenv().KillAuraStart = function()
    Config.Enabled = true
    startLoop()
end
getgenv().KillAuraStop = function() stopLoop(true) end

-- ============ Rayfield Gen2 UI ============
local window = Rayfield:CreateWindow({
    name = "Iron Soul Hub",
    subtitle = "Jiwa Besi: Dungeon | 117533937949084",
    sidebarLayout = true,
    theme = "default",
    configuration = {
        autoSave = true,
        autoLoad = true,
        fileName = "IronSoulHub",
        customFolder = "IronSoul",
    },
})
windowRef = window

-- Sidebar grouping (hanya efek kalau sidebarLayout=true)
window:CreateSection({ name = "General" })

local combatTab = window:CreateTab({ name = "Combat" })
local infoTab = window:CreateTab({ name = "Info" })

-- ---- Combat tab ----
combatTab:CreateSection({ name = "Kill Aura / Auto Attack" })

ui.statusText = combatTab:CreateText({
    name = "Status",
    text = "Idle. Masuk dungeon biar musuh spawn.",
})

local grid = combatTab:CreateGroup()
local left = grid:CreateGroup({ direction = "column" })
local right = grid:CreateGroup({ direction = "column" })

-- Stat wajib di group/row? Stat compact otomatis di row. Pakai default (non-compact) aman di column.
ui.statTargets = left:CreateStat({
    name = "Targets",
    value = 0,
})
ui.statHits = right:CreateStat({
    name = "Total Hits",
    value = getgenv().KillAuraTotalHits or 0,
})

local enableToggle = combatTab:CreateToggle({
    name = "Enable Kill Aura",
    description = "Auto serang musuh terdekat di map.",
    flag = "KillAuraEnabled",
    value = Config.Enabled,
    callback = function(value)
        Config.Enabled = value
        if value then
            startLoop()
            local scope = Config.UnlimitedRange and "seluruh map" or (tostring(Config.Radius) .. " studs")
            window:Notify({ title = "Kill Aura ON", content = "Target " .. scope .. " | " .. tostring(Config.PositionMode) })
        else
            window:Toast({ title = "Kill Aura OFF" })
        end
    end,
})

combatTab:CreateToggle({
    name = "Unlimited Range",
    description = "ON = radius mati, selagi map ada musuh langsung didatangi. OFF = pakai Radius.",
    flag = "KillAuraUnlimited",
    value = Config.UnlimitedRange,
    callback = function(value)
        Config.UnlimitedRange = value
    end,
})

combatTab:CreateSlider({
    name = "Radius",
    description = "Dipakai hanya kalau Unlimited Range OFF.",
    range = { 20, 200 },
    increment = 5,
    value = Config.Radius,
    suffix = "studs",
    flag = "KillAuraRadius",
    callback = function(value)
        Config.Radius = value
    end,
})

combatTab:CreateSlider({
    name = "Jarak Serangan",
    description = "Jarak natural senjata. Pukulan hanya keluar kalau musuh sedekat ini. Samakan dengan Tinggi biar tiap datang langsung mukul.",
    range = { 5, 40 },
    increment = 1,
    value = Config.AttackRange,
    suffix = "studs",
    flag = "KillAuraAttackRange",
    callback = function(value)
        Config.AttackRange = value
    end,
})

combatTab:CreateSlider({
    name = "Attack Delay",
    description = "Jeda antar serangan. Makin kecil makin cepat + makin berisiko.",
    range = { 0.1, 1 },
    increment = 0.05,
    value = Config.AttackDelay,
    suffix = "s",
    flag = "KillAuraDelay",
    callback = function(value)
        Config.AttackDelay = value
    end,
})

combatTab:CreateDropdown({
    name = "Mode Posisi",
    description = "Di Dalam Musuh = sejajar tanah nempel musuh (seperti script yg work). Di Atas Kepala = teleport di atas.",
    options = { "Di Dalam Musuh", "Di Atas Kepala" },
    value = (Config.PositionMode == "Above") and "Di Atas Kepala" or "Di Dalam Musuh",
    flag = "KillAuraPosMode",
    callback = function(selected)
        -- Gen2 single-mode: callback terima string langsung
        local v = type(selected) == "table" and selected[1] or selected
        Config.PositionMode = (v == "Di Atas Kepala") and "Above" or "Inside"
    end,
})

combatTab:CreateSlider({
    name = "Set Distance",
    description = "Jarak player dari musuh (mode Di Dalam Musuh). Meniru script Tora IsMe.",
    range = { 2, 15 },
    increment = 1,
    value = Config.Distance,
    suffix = "studs",
    flag = "KillAuraDistance",
    callback = function(value)
        Config.Distance = value
    end,
})

combatTab:CreateSlider({
    name = "Tinggi Di Atas Musuh",
    description = "Jarak naik-turun di atas kepala musuh.",
    range = { 2, 25 },
    increment = 1,
    value = Config.HeightOffset,
    suffix = "studs",
    flag = "KillAuraHeight",
    callback = function(value)
        Config.HeightOffset = value
    end,
})

combatTab:CreateToggle({
    name = "Auto Skill Q/E/R",
    description = "Otomatis pakai skill Q, E, R bergantian.",
    flag = "KillAuraAutoSkill",
    value = Config.AutoSkill,
    callback = function(value)
        Config.AutoSkill = value
        if value then
            window:Notify({ title = "Auto Skill ON", content = "Q/E/R tiap " .. tostring(Config.SkillDelay) .. "s" })
        end
    end,
})

combatTab:CreateSlider({
    name = "Jeda Skill",
    range = { 1, 10 },
    increment = 0.5,
    value = Config.SkillDelay,
    suffix = "s",
    flag = "KillAuraSkillDelay",
    callback = function(value)
        Config.SkillDelay = value
    end,
})

combatTab:CreateSlider({
    name = "Max Targets",
    description = "Max musuh per tick (cegah lag).",
    range = { 1, 20 },
    increment = 1,
    value = Config.MaxTargets,
    flag = "KillAuraMaxTargets",
    callback = function(value)
        Config.MaxTargets = math.floor(value)
    end,
})

combatTab:CreateToggle({
    name = "Auto Equip Weapon",
    flag = "KillAuraAutoEquip",
    value = Config.AutoEquip,
    callback = function(value) Config.AutoEquip = value end,
})

combatTab:CreateSection({ name = "Dungeon" })

combatTab:CreateToggle({
    name = "Find Chest & Egg",
    description = "Otomatis datangi Chest & Dragon Egg + fire prompt. Jalan kalau tidak ada musuh.",
    flag = "KillAuraChestEgg",
    value = Config.FindChestEgg,
    callback = function(value)
        Config.FindChestEgg = value
        if value then
            startLoop()
            window:Notify({ title = "Chest & Egg ON", content = "Aktif saat tidak ada target musuh." })
        end
    end,
})

combatTab:CreateToggle({
    name = "Kunci Walk Speed",
    flag = "KillAuraWSLock",
    value = Config.WalkSpeedLock,
    callback = function(value)
        Config.WalkSpeedLock = value
        if value then startLoop() end
    end,
})

combatTab:CreateSlider({
    name = "Walk Speed",
    range = { 16, 120 },
    increment = 1,
    value = Config.WalkSpeed,
    flag = "KillAuraWS",
    callback = function(value)
        Config.WalkSpeed = math.floor(value)
    end,
})

-- Keybind + Input + ColorPicker wajib di tab level (tidak boleh di dalam group)
combatTab:CreateKeybind({
    name = "Toggle UI",
    value = Enum.KeyCode.RightShift,
    flag = "UIKeybind",
    callback = function(key)
        -- Gen2: Show/Hide via window handle. Callback key = tombol yg ditekan.
        window:ToggleHide()
    end,
})

combatTab:CreateButton({
    name = "Stop",
    callback = function()
        stopLoop(true)
        -- sync toggle UI tanpa fire callback berulang (skipCallback=true)
        pcall(function() enableToggle:Set(false, true) end)
        window:Notify({ title = "Stopped", content = "Kill Aura mati." })
    end,
})

ui.console = combatTab:CreateConsole({
    name = "Log",
    height = 130,
    follow = true,
    maxLines = 200,
})

-- ---- Info tab ----
infoTab:CreateSection({ name = "Game" })

infoTab:CreateText({
    name = "Iron Soul: Dungeon",
    text = "PlaceId: 117533937949084\nUniverseId: 9910245722\nJobId: 3942a586-f6c0-4591-83f1-c8e3d9aa0313\nUser: Burhan_Gatel (10633534746)\nRemotes: PlayerActionRE, DmgEffectURE, GameRoundRE/MatchRE, DataRE, StatsRE\nCara pakai: masuk MatchRoom/dungeon, nyalakan toggle di tab Combat.",
})

infoTab:CreateButton({
    name = "Copy PlaceId",
    callback = function()
        pcall(function() setclipboard("117533937949084") end)
        window:Toast({ title = "Copied 117533937949084" })
    end,
})

infoTab:CreateButton({
    name = "Notify Test",
    callback = function()
        window:Notify({ title = "Iron Soul Hub", content = "Rayfield Gen2 jalan. Targets update tiap 1 detik." })
    end,
})

-- Sync nilai awal dari autoLoad (kalau config lama Enabled=true, langsung jalan)
task.spawn(function()
    task.wait(0.5)
    -- window.Flags baca live value hasil load
    pcall(function()
        local saved = window.Flags and window.Flags.KillAuraEnabled
        if saved ~= nil then Config.Enabled = saved end
        if window.Flags and window.Flags.KillAuraUnlimited ~= nil then Config.UnlimitedRange = window.Flags.KillAuraUnlimited end
        if window.Flags and window.Flags.KillAuraRadius then Config.Radius = window.Flags.KillAuraRadius end
        if window.Flags and window.Flags.KillAuraAttackRange then Config.AttackRange = window.Flags.KillAuraAttackRange end
        if window.Flags and window.Flags.KillAuraDelay then Config.AttackDelay = window.Flags.KillAuraDelay end
        if window.Flags and window.Flags.KillAuraAbove ~= nil then Config.PlayerAbove = window.Flags.KillAuraAbove end
        if window.Flags and window.Flags.KillAuraPosMode then
            local v = window.Flags.KillAuraPosMode
            v = type(v) == "table" and v[1] or v
            Config.PositionMode = (v == "Di Atas Kepala") and "Above" or "Inside"
        end
        if window.Flags and window.Flags.KillAuraDistance then Config.Distance = window.Flags.KillAuraDistance end
        if window.Flags and window.Flags.KillAuraHeight then Config.HeightOffset = window.Flags.KillAuraHeight end
        if window.Flags and window.Flags.KillAuraAutoSkill ~= nil then Config.AutoSkill = window.Flags.KillAuraAutoSkill end
        if window.Flags and window.Flags.KillAuraSkillDelay then Config.SkillDelay = window.Flags.KillAuraSkillDelay end
        if window.Flags and window.Flags.KillAuraChestEgg ~= nil then Config.FindChestEgg = window.Flags.KillAuraChestEgg end
        if window.Flags and window.Flags.KillAuraWSLock ~= nil then Config.WalkSpeedLock = window.Flags.KillAuraWSLock end
        if window.Flags and window.Flags.KillAuraWS then Config.WalkSpeed = window.Flags.KillAuraWS end
    end)
    if Config.Enabled then startLoop() else startLoop() end -- startLoop idle (tunggu toggle), biar Stat tetap update
    logLine("UI loaded. Config Enabled=" .. tostring(Config.Enabled))
end)

print("[IronSoulHub] Rayfield Gen2 loaded")
return "IronSoulHub Rayfield Gen2 loaded"
